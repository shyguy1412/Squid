import { h } from 'preact';
import { Document } from '@/components/Document';
import { Head } from 'squid-ssr/components';

export default function App({ }: ServerSideProps) {

  return <Document>
    <Head>
      <title>New Page</title>
    </Head>

    Hello World!

  </Document>;

}